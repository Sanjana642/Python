# python writing files (.txt, .json, .csv)

txt_data = 'I Like Pizza!'

file_path = "output.txt"

# w = write mode, x = also write mode when doesn't exists, a = append file, r = read file
with open(file_path, "w") as file:  
    pass
